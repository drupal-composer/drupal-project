# Paragraph type implementing the required interface.
#
# Note that these fields must match the drupal machine_name of the field.
type ListFilterOptionsParagraph implements ParagraphTypeInterface {
  id: String!
  parent_field_name: String!
  field_filter_limit: Int
  field_filter_offset: Int
  field_filter_column: String
  field_sorting_direction: String
}
