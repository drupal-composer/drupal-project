# Custom file, combining development.services.yml and default.services.yml with
# dev settings.

services:
  cache.backend.null:
    class: Drupal\Core\Cache\NullBackendFactory
parameters:
  twig.config:
    debug: true
    auto_reload: null
    cache: true
  http.response.debug_cacheability_headers: true
